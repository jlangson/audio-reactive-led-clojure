(ns audio-reactive-led-strip-clojure.core-test
  (:require [clojure.test :refer :all]
            [audio-reactive-led-strip-clojure.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
